# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
target/
src-tauri/target/
.worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env*

# superpowers brainstorming mockups
.superpowers/

# Apple signing material — never commit. `.env*` above already covers
# .env.release; these patterns stop a stray key copied into the repo root,
# and the negation keeps the credential-less template trackable.
*.p8
*.p12
*.cer
*.certSigningRequest
!.env.release.example
